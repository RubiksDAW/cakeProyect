{
    "name": "pasteleria-ia",
    "version": "1.0.0",
    "description": "Sitio web para pastelería con integración de IA",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "devDependencies": {
        "vite": "^4.4.0"
    }
}